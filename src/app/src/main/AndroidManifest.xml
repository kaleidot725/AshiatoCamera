<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="kaleidot725.highestpeaks">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.location.gps"/>

    <application
            android:name=".ui.MyApplication"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">

        <provider
                android:name="androidx.core.content.FileProvider"
                android:authorities="com.example.android.fileprovider"
                android:exported="false"
                android:grantUriPermissions="true">
            <meta-data
                    android:name="android.support.FILE_PROVIDER_PATHS"
                    android:resource="@xml/file_paths">
            </meta-data>
        </provider>

        <activity
                android:name="kaleidot725.highestpeaks.ui.main.MainActivity"
                android:label="@string/main_title"
                android:screenOrientation="portrait"
                android:theme="@style/AppTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
                android:name="kaleidot725.highestpeaks.ui.setting.SettingActivity"
                android:label="Setting"
                android:screenOrientation="portrait"
                android:parentActivityName="kaleidot725.highestpeaks.ui.main.MainActivity"/>

        <activity
                android:name="kaleidot725.highestpeaks.ui.contact.ContactActivity"
                android:label="Contact"
                android:screenOrientation="portrait"
                android:parentActivityName="kaleidot725.highestpeaks.ui.main.MainActivity"/>

        <activity
                android:name="kaleidot725.highestpeaks.ui.edit.EditActivity"
                android:label="Camera"
                android:screenOrientation="portrait"
                android:parentActivityName="kaleidot725.highestpeaks.ui.main.MainActivity"
                android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name="kaleidot725.highestpeaks.ui.preview.PreviewActivity"
                  android:label="Preview"
                  android:screenOrientation="portrait"
                  android:parentActivityName="kaleidot725.highestpeaks.ui.main.MainActivity"/>
    </application>

</manifest>